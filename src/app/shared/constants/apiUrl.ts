export const ROOT = 'https://shift-backend.onrender.com/api';
export const OTP = `${ROOT}/auth/otp`;
export const USERS = `${ROOT}/users`;
export const SIGNIN = `${USERS}/signin`;
export const PROFILE = `${USERS}/profile`;
export const SESSION = `${USERS}/session`;
